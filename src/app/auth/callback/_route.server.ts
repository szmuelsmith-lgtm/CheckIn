// Auth callback is handled client-side in page.tsx for Capacitor/static builds.
// For server-rendered (Vercel) deployments this file is intentionally left without
// route handler exports so the page.tsx takes precedence.
